{
  "name": "autocomplete",
  "version": "1.0.0",
  "description": "A autocomplete component for inputs",
  "main": "index.js",
  "scripts": {
    "start": "json-server -w ./api/newsletters.json"
  },
  "dependencies": {
    "json-server": "^0.14.0",
    "node-fetch": "^2.2.0"
  }
}
